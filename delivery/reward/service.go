package reward

import (
	"context"
	"marketing/engine/transcation/model"
)

func parseParams(params *model.Params) (*rewardReq, error) {
	// todo...
	return &rewardReq{}, nil
}

func checkLimit(ctx context.Context, r *rewardReq) error {
	// todo...
	return nil
}
